import React from 'react'

const Pagination = ({paginate, cardsPerPage, currentPage}) => {
    const pageNumbers = []

    for (let i = 0; i < array.length; i++) {
        const element = array[i];
        
    }
  return (
    <div>
        
    </div>
  )
}

export default Pagination